<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.5//EN"
	"http://struts.apache.org/dtds/struts-2.5.dtd">

<struts>

	<constant name="struts.enable.DynamicMethodInvocation" value="false" />
	<constant name="struts.devMode" value="true" />
	
	 <!-- 把struts的action对象交给spring容器管理 -->
	<constant name="struts.objectFactory " value="spring" />
	
	 <!-- 允许上传的文件的最大字节数 -->
	<constant name="struts.multipart.maxSize" value="20971520"></constant>
	<!-- 缓存路径 -->
	<constant name="struts.multipart.saveDir" value="/temp" />
	
	 <!-- 后台管理 -->
	 <package name="back" namespace="/backupload" extends="json-default">
		<global-allowed-methods>getPageTypeInfo,addGtypeInfo,updateGtypeInfo</global-allowed-methods>

		<action name="*_*" class="{1}Action" method="{2}">
			<param name="uploadUtil.savePath">../images</param>      <!-- 图片的保存路径 -->
			<result type="json">
				<param name="root">jsonObject</param>
				<param name="encoding">UTF-8</param>
			</result>
		</action>
		</package>
	 
	<package name="backjson" namespace="/backjson" extends="json-default">
		<global-allowed-methods>checkzccode,managerLogin,getPageAdminInfo,addAdminInfo,delAdminInfo,updateAdminInfo,
			getPageUsersInfo,addUsersInfo,
			delGtypeInfo,findGtypeByTid</global-allowed-methods>
		<action name="*_*" class="{1}Action" method="{2}">
			<result type="json">
				<param name="root">jsonObject</param>
				<param name="encoding">UTF-8</param>
			</result>
		</action>
	</package>
</struts>
